[package]
name = "visionpipe-core"
version = "0.1.0"
edition = "2021"
description = "Shared screen capture and metadata library for VisionPipe"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
base64 = "0.22"
chrono = "0.4"
